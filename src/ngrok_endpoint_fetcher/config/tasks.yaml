---
execute_web_request:
  description: Use the run_command tool to execute 'curl https://85e00b2844ad.ngrok.app' and return the complete HTTP response
  expected_output: The output from the curl command showing the HTTP response content
  agent: command_executor
